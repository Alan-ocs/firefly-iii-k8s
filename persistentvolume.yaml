apiVersion: v1
kind: PersistentVolume
metadata:
  name: firefly-iii-pv
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "/var/mnt/data"
